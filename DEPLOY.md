# 🚀 Деплой на Render.com

Инструкция по развертыванию Password Manager на Render.com.

## 📋 Быстрый старт

### Вариант 1: Автоматический деплой через Dashboard

1. **Создайте аккаунт на Render.com**
   - Перейдите на https://render.com
   - Зарегистрируйтесь или войдите через GitHub

2. **Создайте новый Web Service**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий или загрузите код

3. **Настройте проект**
   ```
   Name: password-manager (или любое другое имя)
   Environment: Docker
   Region: Выберите ближайший регион
   Branch: main (или ваша основная ветка)
   ```

4. **Настройте переменные окружения**
   
   В разделе "Environment Variables" добавьте:
   ```
   BASE_URL = https://your-app-name.onrender.com
   ALLOWED_ORIGIN = *
   ```
   
   Примечание: `PORT` автоматически установится Render

5. **Деплой**
   - Нажмите "Create Web Service"
   - Render автоматически соберет Docker образ и запустит приложение
   - Дождитесь завершения деплоя (~5-10 минут)

6. **Готово!**
   - Ваше приложение будет доступно по адресу: `https://your-app-name.onrender.com`

### Вариант 2: Деплой через Blueprint (render.yaml)

1. **Загрузите код в GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/your-username/password-manager.git
   git push -u origin main
   ```

2. **Создайте Blueprint на Render**
   - Перейдите на https://dashboard.render.com/blueprints
   - Нажмите "New Blueprint Instance"
   - Подключите ваш GitHub репозиторий
   - Render автоматически найдет `render.yaml` и применит конфигурацию

3. **Обновите BASE_URL**
   - После деплоя обновите переменную `BASE_URL` на актуальный URL вашего приложения

## 🔧 Локальное тестирование Docker

Перед деплоем можно протестировать Docker локально:

```bash
# Соберите образ
docker build -t password-manager .

# Запустите контейнер
docker run -p 8080:8080 \
  -e PORT=8080 \
  -e BASE_URL=http://localhost:8080 \
  -e ALLOWED_ORIGIN="*" \
  password-manager

# Откройте браузер
# http://localhost:8080
```

## 🌐 Настройка собственного домена (опционально)

1. В Render Dashboard перейдите в настройки вашего сервиса
2. Раздел "Custom Domain"
3. Добавьте ваш домен
4. Обновите DNS записи у вашего регистратора:
   ```
   CNAME your-domain.com → your-app-name.onrender.com
   ```
5. Обновите переменную `BASE_URL`:
   ```
   BASE_URL = https://your-domain.com
   ```

## ⚙️ Переменные окружения

| Переменная | Описание | Обязательная | Пример |
|------------|----------|--------------|--------|
| `PORT` | Порт сервера | Да (авто) | `8080` |
| `BASE_URL` | Базовый URL приложения | Да | `https://your-app.onrender.com` |
| `ALLOWED_ORIGIN` | CORS origins | Нет | `*` или `https://your-domain.com` |

## 📊 Мониторинг

- **Логи**: Render Dashboard → Logs
- **Метрики**: Render Dashboard → Metrics
- **Health Check**: `/api/health` endpoint

## 💰 Тарифы

- **Free Tier**: 
  - ✅ Бесплатно
  - ⏱️ Засыпает после 15 минут неактивности
  - 🐌 Первый запрос после сна ~30 секунд
  - 💾 750 часов в месяц

- **Starter ($7/месяц)**:
  - ✅ Всегда активен
  - ⚡ Быстрый запуск
  - 🚀 Больше ресурсов

## 🔒 Безопасность в продакшене

### Рекомендации:

1. **HTTPS обязательно**
   - Render автоматически предоставляет SSL сертификат

2. **Ограничьте CORS**
   ```
   ALLOWED_ORIGIN = https://your-exact-domain.com
   ```

3. **Регулярные обновления**
   - Обновляйте зависимости Go
   - Следите за обновлениями безопасности

4. **Мониторинг**
   - Настройте алерты в Render
   - Проверяйте логи на подозрительную активность

## 🐛 Troubleshooting

### Проблема: Приложение не запускается

**Решение**:
1. Проверьте логи в Render Dashboard
2. Убедитесь, что Docker образ собрался без ошибок
3. Проверьте переменные окружения

### Проблема: CORS ошибки

**Решение**:
```
ALLOWED_ORIGIN = *
```
или укажите точный домен вашего фронтенда

### Проблема: 404 для статических файлов

**Решение**:
- Убедитесь, что папка `frontend/` копируется в Docker образ
- Проверьте путь в `main.go` (должен быть `./frontend` или `/app/frontend`)

### Проблема: Долгий первый запрос (Free tier)

**Решение**:
- Это нормально для Free tier (холодный старт)
- Для production используйте платный план
- Или настройте ping-сервис (например, UptimeRobot)

## 📱 После деплоя

1. **Проверьте работоспособность**
   ```
   curl https://your-app.onrender.com/api/health
   ```

2. **Протестируйте все функции**
   - Добавление паролей
   - Генерация паролей
   - Создание share ссылок
   - Открытие share ссылок

3. **Добавьте в закладки** 🔖

## 🎉 Готово!

Ваш Password Manager теперь доступен онлайн!

**URL**: `https://your-app-name.onrender.com`

---

Нужна помощь? Проверьте [документацию Render](https://render.com/docs) или логи в Dashboard.
