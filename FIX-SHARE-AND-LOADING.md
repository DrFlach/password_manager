✅ ИСПРАВЛЕНО: Share страница и загрузка паролей

## Проблемы:
1. ❌ Share ссылка перенаправляла на главную страницу
2. ❌ Пароли не отображались до создания нового

## Что исправлено:

### 1. Share страница (app.js)
**Было:**
- `checkSharePage()` не возвращала результат
- `init()` продолжал загружать обычный интерфейс даже для share страниц

**Стало:**
```javascript
async checkSharePage() {
    if (match) {
        await this.loadSharedPassword(token);
        return true; // Это share страница
    }
    return false; // Обычная страница
}

async init() {
    const isSharePage = await this.checkSharePage();
    if (isSharePage) {
        return; // Останавливаем инициализацию для share
    }
    // ... загружаем обычный интерфейс
}
```

### 2. Загрузка паролей (app.js)
**Было:**
- `loadPasswords()` вызывался без `await`
- Рендеринг начинался до завершения дешифровки

**Стало:**
```javascript
await this.loadPasswords(); // Ждем загрузки
this.setupEventListeners();
this.renderPasswords(); // Рендерим после загрузки
```

## Результат:
✅ Share ссылки теперь показывают правильную страницу с паролем
✅ Пароли отображаются сразу при загрузке страницы

## Деплой:
```
✅ Закоммичено
✅ Отправлено на GitHub  
⏳ Render задеплоит через ~3-5 минут
```

Проверьте через несколько минут! 🚀
