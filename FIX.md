# 🔧 ИСПРАВЛЕНИЕ: Share ссылки теперь работают!

## Что было исправлено:

### ✅ Проблема
Share ссылки генерировались с `localhost` вместо реального домена Render.

### ✅ Решение
Фронтенд теперь **сам формирует правильный URL** на основе текущего домена:
```javascript
const shareUrl = `${window.location.protocol}//${window.location.host}/share/${data.token}`;
```

## 📤 Как задеплоить исправление на Render:

### Вариант 1: Автоматический скрипт
```bash
cd /data/password_manager
./deploy.sh
```

### Вариант 2: Вручную
```bash
git add .
git commit -m "Fix: Share links now use correct domain"
git push origin main
```

Render автоматически подхватит изменения и задеплоит через ~3-5 минут.

## 🧪 Проверка после деплоя:

1. Откройте ваше приложение на Render
2. Создайте тестовый пароль
3. Нажмите кнопку "Share" (📤)
4. Создайте ссылку
5. Проверьте, что ссылка начинается с вашего домена:
   ```
   ✅ https://password-manager-63d3.onrender.com/share/...
   ❌ http://localhost:8080/share/...
   ```

## 📋 Настройка Render (опционально):

Вам больше **НЕ нужна** переменная `BASE_URL` на Render!

Если она есть - можете удалить:
1. Render Dashboard → Ваш сервис
2. Environment → Environment Variables
3. Удалите `BASE_URL` (если есть)
4. Save Changes

## 🎯 Что изменилось в коде:

### `frontend/app.js`
- Теперь формирует share URL на клиенте
- Автоматически использует правильный домен

### `render.yaml`
- Удалена переменная `BASE_URL` (больше не нужна)

## ✅ Готово!

После деплоя share ссылки будут работать корректно с вашим доменом Render.com! 🎉
